/**
 * Chapter route layout
 * 
 * Props :
 * - children (React.ReactDOM): React DOM element
 */
const ChapterLayout = ({children}) => {
    <>
        {children}
    </>
}

export default ChapterLayout;