/**
 * Series route layout
 * 
 * Props :
 * - children (React.ReactDOM): React DOM element
 */
const SeriesLayout = ({children}) => {
    <>
        {children}
    </>
}

export default SeriesLayout;