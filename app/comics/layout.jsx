/**
 * Comics route layout
 * 
 * Props :
 * - 'children' (React.ReactDOM): React DOM element  
 */

const ComicsLayout = ({children}) => {
    <>
        {children}
    </>
}

export default ComicsLayout;