/**
 * Search route layout
 * 
 * Props :
 * - children (React.ReactDOM): React DOM element
 */
const SearchLayout = ({children}) => {
    <>
        {children}
    </>
}

export default SearchLayout;