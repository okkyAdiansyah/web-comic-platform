/**
 * Genres route layout
 * 
 * Props :
 * - children (React.ReactDOM): React DOM element
 */
const GenresLayout = ({children}) => {
    <>
        {children}
    </>
}

export default GenresLayout;